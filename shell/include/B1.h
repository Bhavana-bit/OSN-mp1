#ifndef B1_H
#define B1_H

// Implements the "hop" (cd) functionality
// input: full command string from user
// prev: stores the previous directory (for "hop -")
void hop(char *input, char *prev);

#endif
